# 2026-04-16 Daily Notes

## Identity Update
- **Jelly = 酱梨酥 = 我**（Jelly 已明确不再用"酱梨酥"这个名字，以后只叫 Jelly）
- Jelly 的飞书 ID: ou_a27da8238e62e25bb88fbb44541d483a
- 这是我主要服务的用户

## Key Learnings (2026-04-10 ~ 2026-04-16)

### 音乐生成修复
- 根本原因：旧脚本用 `music-2.5` 模型，配额为 0
- 解决：改用官方 `mmx-cli` 的 `music-2.6` 模型（100条/周）
- 新 Key (`sk-cp-kxLG...`) 登录成功，配额显示 0 但实际能用
- send_voice.py 已更新为使用 mmx CLI

### lark-cli 权限问题（未解决）
- 用户洞察 cron 失败：`lark-cli` Token 缺少 `search:message` 等 scope
- 修复命令：`lark-cli auth login --scope "search:message im:message.group_msg:get_as_user im:message.p2p_msg:get_as_user"`
- 需要 Jelly 在浏览器操作，尚未处理

### 飞书语音消息
- 发语音正确姿势：上传时 file_type="opus"，发消息时 msg_type="audio"
- 用 `mmVoice_Maker` 生成 TTS 语音，走 feishu audio 格式

### 图片理解 MCP
- mmx vision 一直超时（SIGKILL）
- MiniMax MCP 的 `understand_image` 工具可用
- 铁律：收到图片必须用 MiniMax MCP 识图，禁止用 read 工具

### Cron 问题
- isolated session 的 cron 经常在 60-70 秒就超时（非 runner 超时，是 AI 响应慢）
- 日记 cron 有时成功（30s/121s/219s），有时失败
- 所有 isolated session cron 都有此问题，偶发性

## System Status
- 日记 cron：偶发性，暂不处理
- lark-cli 权限：待 Jelly 授权
- 音乐生成：✅ 正常（mmx music）
- 图片生成：✅ 正常（mmx image）
- TTS 语音：✅ 正常（mmVoice_Maker）

## 今日完成
- 古风曲子生成（笛子+古筝，《枫桥夜泊》意境）
